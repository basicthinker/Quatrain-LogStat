Directory "server":
  List target log file names in file.list.
  Run the shell script run.sh.

Directory "client":
  Save the output log of sequential requests as a file "SR.log".
  Run the shell script run.sh.
